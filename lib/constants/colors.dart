import 'package:flutter/material.dart';

// All colors being used in the app.
class CustomColors {
  /* 
  pratham change the colors accordingly. These are also being used in themes 
  file so that the app changes themes based on the device settings.
  */
  static const Color gradient_color1 = Color(0xFFA0A0FF);
  static const Color gradient_color2 = Color(0xFF6D6AFA);

  static const Color primary_dark = Color(0xFF1F2730);
  static const Color primary_light = Color.fromARGB(255, 255, 255, 255);

  static const Color secondary_dark = Color(0xFF363535);
  static const Color secondary_light = Color(0xFFD4D4D4);

  static const Color third_dark = Color(0xFF211F32);

  static const Color light_blue = Color(0xFFF7F5FF);
  static const Color dark_grey = Color(0xFF8A8A8A);
  static const Color light_grey = Color(0xFFE6E6E6);
  static const Color error = Color.fromARGB(255, 255, 0, 0);
  static const Color success = Color.fromARGB(255, 40, 254, 21);
}
